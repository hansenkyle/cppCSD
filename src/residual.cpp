#include "residual.h"

// residualNorm() is a template, so its definition lives entirely in
// residual.h; this file exists to keep the header/source pairing this
// codebase uses elsewhere, and to make sure the header compiles standalone.
